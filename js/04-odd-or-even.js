// ITERATE FROM 0 TO 15 USING REMAINDER OPERATOR TO CHECK IF EVEN OR ODD
for (i = 0; i <= 15; i++) {
  if (i % 2 === 0) {
    console.log(`${i} is even`);
  } else {
    console.log(`${i} is odd`);
  }
}
